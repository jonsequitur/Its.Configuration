install:
  - nuget restore

configuration:
  - Release

build:
  project: Its.Configuration.sln
  publish_nuget: true

test:
  assemblies:
    - '**\*Tests.dll'

artifacts:
  - path: Configuration.Console\bin
    name: Its.Configuration.Console

  - path: its.config.js